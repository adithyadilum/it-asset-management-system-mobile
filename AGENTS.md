# Expo SDK version

This project is on **Expo SDK 54** (`expo: ~54.0.35` in package.json).

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code. Expo's API surface changes between majors, and guidance from a
newer SDK will not apply here.

If the SDK is upgraded, update this file and the link in the same change.
